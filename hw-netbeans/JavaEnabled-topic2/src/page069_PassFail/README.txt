PassFail.java 

    Prompts the user for two scores, applies an algorithm, and reports whether the user
    passed or failed.

PassFail2.java 

    Another version where the code is run from the main() method instead of the constructor.

PassMark.java 